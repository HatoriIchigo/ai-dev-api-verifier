#
# filename: openapi.yaml
# description: テスト用 spec
# created_by: tester
# created_at: 2026-06-16
# status: approved
#

openapi: 3.0.3
# 'info' オブジェクトを意図的に欠落させ、redocly lint のみを失敗させる。
# operationId / x-internal は揃えてあるため operations チェックは成功する。
servers:
  - url: https://api.real.example.org
paths:
  /health:
    get:
      operationId: getHealth
      x-internal: false
      responses:
        '200':
          description: OK

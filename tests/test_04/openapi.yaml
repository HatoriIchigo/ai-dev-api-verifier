#
# filename: openapi.yaml
# description: backendのIF仕様書
# created_by: tester
# created_at: 2026-06-16
# status: approved
#

openapi: 3.0.3
# x-internal を欠落させ、operations チェックのみを失敗させるケース。
info:
  title: Missing X-Internal API
  version: 1.0.0
servers:
  - url: https://api.example.org
security:
  - bearerAuth: []
paths:
  /health:
    get:
      summary: Health check
      operationId: getHealth
      responses:
        '200':
          description: OK
        '400':
          description: Bad Request
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

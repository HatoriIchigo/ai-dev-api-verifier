#
# filename: openapi.yaml
# description: backendのIF仕様書
# created_by: tester
# created_at: 2026-06-16
# status: approved
#

openapi: 3.0.3
# summary を欠落させ、redocly(operation-summary) のみを失敗させるケース。
# operations / frontmatter は成功する。
info:
  title: Missing Summary API
  version: 1.0.0
servers:
  - url: https://api.example.org
security:
  - bearerAuth: []
paths:
  /health:
    get:
      operationId: getHealth
      x-internal: false
      responses:
        '200':
          description: OK
        '400':
          description: Bad Request
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
